server:
  port: ${CONFIG_PORT:8888}

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/PhuNguyen12345/fu_car_renting_system.git
          default-label: ${CONFIG_BRANCH:main}
          search-paths: infra/config-repo

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/